<mah:MetroWindow x:Class="ForgeAir.Playout.Views.Settings.Ads.AdTracksBrowserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ForgeAir.Playout.Views.Settings.Ads"
        mc:Ignorable="d"
        Title="Select Track" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <ListBox DisplayMemberPath="Title" ScrollViewer.CanContentScroll="True" BorderThickness="0" Margin="10" SelectedItem="{Binding SelectedTrack}" ItemsSource="{Binding Tracks}" Background="{x:Null}" Grid.Row="0">

        </ListBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,10">
            <Button Content="Cancel" Margin="0,0,10,0" x:Name="Cancel"/>
            <Button Content="OK" IsDefault="True" x:Name="Ok"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
