<mah:MetroWindow x:Class="ForgeAir.Playout.Views.Settings.Ads.AdPackListView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 Title="Ad Packs"
                 Height="450"
                 Width="800">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- LIST -->
        <DataGrid x:Name="AdPacks"
                  SelectedItem="{Binding SelectedAdPack}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="*"/>

                <DataGridCheckBoxColumn Header="Enabled"
                                        Binding="{Binding IsActive}"
                                        Width="100"/>

                <DataGridCheckBoxColumn Header="Repeat Daily"
                                        Binding="{Binding RepeatEveryDay}"
                                        Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <Button x:Name="CreatePack"
                    Content="New"
                    Width="100"
                    Margin="0,0,10,0"/>

            <Button x:Name="EditPack"
                    Content="Edit"
                    Width="100"
                    Margin="0,0,10,0"/>

            <Button x:Name="DeletePack"
                    Content="Delete"
                    Width="100"/>
        </StackPanel>

    </Grid>
</mah:MetroWindow>
