<mah:MetroWindow
    x:Class="ForgeAir.Playout.Views.StationSelectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://caliburnmicro.com"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:ForgeAir.Playout.Views"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrols="clr-namespace:ForgeAir.UI.Core.Controls;assembly=ForgeAir.UI.Core"
    Title="Choose Station"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Name="textBlock"
                Margin="10,0,0,0"
                FontSize="36"
                Foreground="Gainsboro"
                Text="Choose a Station"
                TextAlignment="Center"
                TextWrapping="Wrap" />
            <ListBox
                Grid.Row="1"
                Background="{x:Null}"
                BorderThickness="0"
                ItemsSource="{Binding Stations}"
                Style="{DynamicResource WrapPanelHorizontalListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="data:TileModel">
                        <mah:Tile
                            Title="{Binding DisplayName}"
                            Command="{Binding DataContext.Command, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}"
                            Foreground="White">
                            <Image Source="{Binding LogoPath}" Stretch="UniformToFill" />
                        </mah:Tile>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <mah:Tile
            Title="Create New Station"
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Background="Green">
            <iconPacks:PackIconRemixIcon
                Width="40"
                Height="40"
                Kind="AddBoxFill" />
        </mah:Tile>

    </StackPanel>

</mah:MetroWindow>
