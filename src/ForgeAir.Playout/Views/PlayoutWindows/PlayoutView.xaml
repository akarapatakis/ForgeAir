<UserControl x:Class="ForgeAir.Playout.Views.PlayoutWindows.PlayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:UserControls="clr-namespace:ForgeAir.Playout.UserControls"
             xmlns:local="clr-namespace:ForgeAir.Playout.Views.PlayoutWindows"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"

             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Queue" Grid.Column="0" Margin="5">
                <ContentControl cal:View.Model="{Binding TrackQueueViewModel}" />
            </GroupBox>

            <GroupBox Header="Track Library" Grid.Column="1" Margin="5">
                <ContentControl cal:View.Model="{Binding TrackSelectorViewModel}" />
            </GroupBox>

        </Grid>

        <GroupBox Header="On Air" Grid.Row="1" Margin="10">
            <ContentControl cal:View.Model="{Binding OnAirViewModel}" />
        </GroupBox>
    </Grid>
</UserControl>
