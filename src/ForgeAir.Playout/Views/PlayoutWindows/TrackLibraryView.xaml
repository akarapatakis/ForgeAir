<UserControl
    x:Class="ForgeAir.Playout.Views.PlayoutWindows.TrackLibraryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:ForgeAir.Playout.Views.PlayoutWindows"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="21*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />

            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="79*" />
            </Grid.RowDefinitions>
            <TextBox
                x:Name="SearchBox"
                Grid.Row="0"
                Margin="10,28,186,28"
                mah:TextBoxHelper.ClearTextButton="True"
                mah:TextBoxHelper.IsMonitoring="True"
                mah:TextBoxHelper.IsWaitingForData="True"
                mah:TextBoxHelper.Watermark="Search"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <ListView
                x:Name="Tracks"
                Grid.Row="1"
                ItemsSource="{Binding Items}"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.IsDeferredScrollingEnabled="True"
                SelectedItem="{Binding SelectedTrack}"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Standard">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="{Binding Background}" />
                        <Setter Property="Foreground" Value="{Binding Foreground}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            Width="80"
                            DisplayMemberBinding="{Binding DisplayType}"
                            Header="" />

                        <GridViewColumn
                            Width="50"
                            DisplayMemberBinding="{Binding Id}"
                            Header="ID" />

                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding Title}"
                            Header="Title" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding DisplayArtists}"
                            Header="Artists" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding Album}"
                            Header="Album" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding DisplayDuration}"
                            Header="Duration" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding DisplayCategories}"
                            Header="Categories" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding DisplayDateAdded}"
                            Header="Date Added" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding DisplayDateModified}"
                            Header="Date Modified" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding DisplayAddedBy}"
                            Header="Added By" />


                    </GridView>
                </ListView.View>
            </ListView>

            <ComboBox
                x:Name="TrackTypeList"
                Grid.Row="0"
                Margin="10,28,10,28"
                HorizontalAlignment="Right"
                ItemsSource="{Binding ComboItems}"
                SelectedItem="{Binding SelectedTrackType}" />

        </Grid>
        <StackPanel Grid.Column="0">
            <mah:Tile Title="Import Tracks" Background="BlueViolet">
                <iconPacks:RemixIcon
                    Width="80"
                    Height="80"
                    Kind="ImportFill" />
            </mah:Tile>
            <mah:Tile Title="Monitor" Background="Orange">
                <iconPacks:RemixIcon
                    Width="80"
                    Height="80"
                    Kind="HeadphoneFill" />
            </mah:Tile>
            <mah:Tile Title="Remove Selected" Background="IndianRed">
                <iconPacks:RemixIcon
                    Width="80"
                    Height="80"
                    Kind="DeleteBinFill" />
            </mah:Tile>
        </StackPanel>
    </Grid>
</UserControl>
