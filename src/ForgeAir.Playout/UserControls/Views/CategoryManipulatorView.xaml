<UserControl x:Class="ForgeAir.Playout.UserControls.Views.CategoryManipulatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ForgeAir.Playout.UserControls.Views"
                       xmlns:cal="http://caliburnmicro.com"

             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="407*"/>
            <ColumnDefinition Width="14*"/>
            <ColumnDefinition Width="379*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="68*"/>
            <RowDefinition Height="31*"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="SelectedCategory" Margin="0,0,4,0" Grid.ColumnSpan="3" ItemsSource="{Binding FetchedDTOCategories}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" DisplayMemberPath="Name" />
        <ListBox x:Name="SelectedCategories" ItemsSource="{Binding SelectedCategories}" Margin="0,5,0,5" Grid.Row="1" Grid.ColumnSpan="3" DisplayMemberPath="Name" SelectedItem="{Binding SelectedAddedCategory, Mode=TwoWay}"/>
        <Button cal:Message.Attach="[Event Click] = [Action Add]" Content="Add" Margin="0,3,14,0" Grid.Row="2" VerticalAlignment="Top" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Button cal:Message.Attach="[Event Click] = [Action Remove]" Content="Remove" Margin="0,3,0,0" VerticalAlignment="Top" Grid.Row="2" Grid.Column="2"/>

    </Grid>
</UserControl>
    