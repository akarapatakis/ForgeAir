<UserControl x:Class="ForgeAir.Playout.UserControls.PlaybackQueue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:local="clr-namespace:ForgeAir.Playout.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <ListView x:Name="queueBox"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch"
                  hc:ScrollViewerAttach.Orientation="Vertical">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content=">>" Click="skipButton_click"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Place" Width="55" DisplayMemberBinding="{Binding Place}" />
                    <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding DisplayTitle}" />
                    <GridViewColumn Header="Artists" Width="200" DisplayMemberBinding="{Binding DisplayArtists}" />
                    <GridViewColumn Header="Duration" Width="80" DisplayMemberBinding="{Binding DisplayDuration}" />
                    <GridViewColumn Header="" Width="90">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="REMOVE" Click="removeFromQueue"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
