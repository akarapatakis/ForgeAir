<UserControl x:Class="ForgeAir.Playout.Views.PlayoutWindows.ActivityCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ForgeAir.Playout.Views.PlayoutWindows"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"  Height="720" Width="1280">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="textBlock" TextAlignment="Center" FontSize="36" TextWrapping="Wrap" Text="{Binding StationName}" Margin="10,0,0,0" Foreground="Gainsboro"/>


        <ListBox BorderThickness="0" Style="{DynamicResource WrapPanelHorizontalListBox}" ItemsSource="{Binding DataList}" Background="{x:Null}" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="data:TileModel">
                    <mah:Tile Title="{Binding Title}" Foreground="White" Command="{Binding Command}"  Background="{Binding Color}">
                        <iconPacks:PackIconRemixIcon Kind="{Binding Icon}" Width="40" Height="40" />
                    </mah:Tile>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
