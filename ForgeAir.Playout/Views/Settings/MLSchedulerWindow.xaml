<hc:Window x:Class="ForgeAir.Playout.Views.Settings.MLSchedulerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:ForgeAir.Playout.Views.Settings"
        mc:Ignorable="d"
        Title="Machine Learning Pilot Scheduler" Height="450" Width="800">
    <Grid>
        <GroupBox Header="GroupBox">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="172*"/>
                    <RowDefinition Height="23*"/>
                </Grid.RowDefinitions>


                <hc:TimePicker HorizontalAlignment="Left" Margin="559,181,0,135" Height="26" Width="176"/>
                <DataGrid>
                    <GridView>
                        <GridViewColumn Header="" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <hc:TimePicker SelectedTime="{x:Static system:DateTime.Now}"  Width="220" HorizontalAlignment="Stretch"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <hc:TimePicker SelectedTime="{x:Static system:DateTime.Now}"  Width="220" HorizontalAlignment="Stretch"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <hc:TimePicker SelectedTime="{x:Static system:DateTime.Now}"  Width="220" HorizontalAlignment="Stretch"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </DataGrid>
            </Grid>
        </GroupBox>

    </Grid>
</hc:Window>
