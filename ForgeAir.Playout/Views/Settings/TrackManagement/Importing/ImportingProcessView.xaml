<Window x:Class="ForgeAir.Playout.Views.ImportingProcessView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:ForgeAir.Playout.Views"
        mc:Ignorable="d"
        Title="Importing Tracks" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="336*"/>

            <RowDefinition Height="33*"/>
            <RowDefinition Height="65*"/>
        </Grid.RowDefinitions>
        <ListView x:Name="listView"  VirtualizingStackPanel.IsVirtualizing="True" Margin="0,0,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Import Status" DisplayMemberBinding="{Binding ImportTrackStatus}" Width="200"/>
                    <GridViewColumn Header="FilePath" DisplayMemberBinding="{Binding Title}" Width="400"/>
                    <GridViewColumn Header="Error(s)" DisplayMemberBinding="{Binding ImportTrackErrors}" Width="400"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ProgressBar Margin="0,4,0,9" Grid.Row="1"/>
        <Button Content="Done" HorizontalAlignment="Stretch" Margin="676,28,10,9" Grid.Row="2" VerticalAlignment="Stretch" IsEnabled="False"/>
    </Grid>
</Window>
