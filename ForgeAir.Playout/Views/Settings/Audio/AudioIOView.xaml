<mah:MetroWindow
    x:Class="ForgeAir.Playout.Views.Settings.Audio.AudioIOView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    Title="Audio Settings"
    Width="800"
    Height="500">
    <DockPanel Margin="10">
        <TextBlock
            Margin="0,0,0,10"
            HorizontalAlignment="Center"
            DockPanel.Dock="Top"
            FontSize="28"
            FontWeight="Light"
            Text="Audio Configuration" />

        <TabControl>

            <TabItem Header="Input/Output">
                <StackPanel Margin="10">
                    <TextBlock Margin="0,0,0,5" Text="Audio Engine" />
                    <ComboBox
                        Width="300"
                        Margin="0,0,0,10"
                        ItemsSource="{Binding AudioEngines}"
                        SelectedItem="{Binding SelectedAudioEngine}" />

                    <TextBlock Margin="0,0,0,5" Text="MIC/Line Main Input" />
                    <ComboBox
                        Width="300"
                        Margin="0,0,0,10"
                        ItemsSource="{Binding InputDevices}"
                        SelectedItem="{Binding SelectedInputDevice}" />

                    <TextBlock Margin="0,10,0,5" Text="Main Output" />
                    <ComboBox
                        Width="300"
                        Margin="0,0,0,10"
                        ItemsSource="{Binding OutputDevices}"
                        SelectedItem="{Binding SelectedOutputDevice}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate DataType="data:TileModel">
                                <TextBlock Text="{Binding Name}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </TabItem>

            <TabItem Header="Advanced">
                <StackPanel Margin="10">
                    <TextBlock Margin="0,0,0,5" Text="Device Buffer" />
                    <TextBox Text="333" />
                    <Button Margin="0,100,0,0" Content="VST Plugins" />

                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            DockPanel.Dock="Bottom"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="5,0"
                Command="{Binding SaveCommand}"
                Content="Save" />
            <Button
                Width="100"
                Margin="5,0"
                Command="{Binding CancelCommand}"
                Content="Cancel" />
        </StackPanel>
    </DockPanel>
</mah:MetroWindow>
