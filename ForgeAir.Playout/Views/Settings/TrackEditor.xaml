<hc:Window x:Class="ForgeAir.Playout.Views.Settings.TrackEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"

        xmlns:local="clr-namespace:ForgeAir.Playout.Views.Settings"
        mc:Ignorable="d"
        Title="Edit Track" Height="450" Width="800">

    <Grid>
        <Grid.Background>
            <VisualBrush Stretch="UniformToFill">
                <VisualBrush.Visual>
                    <Grid ClipToBounds="True">
                        <Image x:Name="coverblur" Source="pack://application:,,,/ForgeAir.UI.Core;component/Assets/Icons/playout/nocover_fallback.png" Opacity="0.5">
                            <Image.Effect>
                                <BlurEffect Radius="10"/>
                            </Image.Effect>
                        </Image>
                    </Grid>
                </VisualBrush.Visual>
            </VisualBrush>

        </Grid.Background>
        <hc:Watermark Mark="ForgeAir" FontSize="32" FontWeight="UltraLight" Opacity="0.5"/>
        <GroupBox x:Name="groupBox" Header="Edit Track" Background="Transparent">
            <Grid>
                <hc:TextBox x:Name="titleBox" Margin="10,60,10,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top"/>
                <hc:ButtonGroup Margin="0,0,19,10" Style="{StaticResource ButtonGroupSolid}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="230">
                    <Button x:Name="addButton" Content="OK" IsDefault="True" Style="{StaticResource ButtonDefault}" HorizontalAlignment="Right" Width="100" Click="addButton_Click"/>
                    <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Right" Width="100" Click="cancelButton_Click"/>
                </hc:ButtonGroup>
                <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="10,39,0,0" TextWrapping="Wrap" Text="Title" VerticalAlignment="Top"/>
                <TextBlock x:Name="textBl1ock" HorizontalAlignment="Left" Margin="10,126,0,0" TextWrapping="Wrap" Text="Artists" VerticalAlignment="Top"/>
                <ListBox x:Name="artistsBox" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="10,147,0,108" HorizontalAlignment="Left" Width="280"/>
                <hc:TextBox x:Name="albumBox" Margin="311,155,11,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top"/>
                <TextBlock x:Name="textBlock_Copy" HorizontalAlignment="Left" Margin="311,134,0,0" TextWrapping="Wrap" Text="Album" VerticalAlignment="Top" Width="84"/>
                <hc:TextBox x:Name="isrcBox" Margin="311,216,11,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top"/>
                <TextBlock x:Name="textBlock_Copy1" HorizontalAlignment="Left" Margin="311,195,0,0" TextWrapping="Wrap" Text="ISRC" VerticalAlignment="Top" Width="84"/>
                <CheckBox x:Name="enabledbox" Content="Track Enabled" HorizontalAlignment="Left" Margin="311,249,0,0" VerticalAlignment="Top"/>
                <TextBlock x:Name="textBl1ock_Copy" HorizontalAlignment="Left" Margin="10,293,0,0" TextWrapping="Wrap" Text="Categories" VerticalAlignment="Top" Height="13" Width="280"/>
                <ListBox x:Name="categoriesBox" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="10,314,0,10" HorizontalAlignment="Left" Width="280"/>

            </Grid>
        </GroupBox>
    </Grid>
</hc:Window>
