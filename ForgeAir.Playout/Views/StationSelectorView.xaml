<Window x:Class="ForgeAir.Playout.Views.StationSelectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cal="http://caliburnmicro.com"
        xmlns:usercontrols="clr-namespace:ForgeAir.UI.Core.Controls;assembly=ForgeAir.UI.Core"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:ForgeAir.Playout.Views"
        mc:Ignorable="d"
        Title="Choose Station" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="textBlock" TextAlignment="Center" FontSize="36" TextWrapping="Wrap" Text="Choose a Station" Margin="10,0,0,0" Foreground="Gainsboro"/>
        <ListBox BorderThickness="0" Style="{DynamicResource WrapPanelHorizontalListBox}" ItemsSource="{Binding Stations}" Background="{x:Null}" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="data:TileModel">
                    <mah:Tile Title="{Binding Tag}" Foreground="White" Command="{Binding DataContext.Command, RelativeSource={RelativeSource AncestorType=ListBox}}"
                    CommandParameter="{Binding}"  >
                        <iconPacks:PackIconRemixIcon Kind="Radio2Fill" Width="40" Height="40" />
                    </mah:Tile>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
